{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/app"
        }
    ],
    "installCommand": "npm install --omit=dev",
    "devCommand": "concurrently \"npm run next-dev\" \"npm run api-dev\"",
    "buildCommand": "next build"
}